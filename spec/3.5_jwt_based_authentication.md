Add JWT-based authentication to the existing Spring Boot application.

Requirements:
- Create a login API: POST /api/v1/auth/login
- Accept username and password
- Validate against User table
- Generate JWT token on successful login
- Include userId and username in token claims
- Token expiration: 1 hour

- Secure all other APIs using JWT filter
- Use Spring Security with JWT filter
- Return token in response
